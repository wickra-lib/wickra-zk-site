version: 2
updates:
  # The site's own npm dependencies: VitePress and Vue, the two Vite plugins the
  # WASM demo needs, the badge renderer (sharp, resvg).
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: monthly
      time: "06:00"
      timezone: "Etc/UTC"
    open-pull-requests-limit: 10
    cooldown:
      default-days: 7
    commit-message:
      prefix: "deps(npm)"
    groups:
      site:
        patterns: ["*"]

  # The 3 workflows here (links, refresh-badges, zizmor) pin their actions by
  # full SHA, which only something watching them will ever move.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: monthly
      time: "06:00"
      timezone: "Etc/UTC"
    open-pull-requests-limit: 10
    cooldown:
      default-days: 7
    commit-message:
      prefix: "deps(actions)"
    groups:
      github-actions:
        patterns: ["*"]
